import { StyledHome } from './../home/Home.style';
import styled from 'styled-components';

export const StyledBookSearch = styled(StyledHome)`
    .booksList__title {
        span {
            font-weight: normal;
        }
    }
`;
